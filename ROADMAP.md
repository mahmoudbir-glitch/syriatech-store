# SYRIATECH — خطة الإصدار الثاني

آخر تحديث: 2026-09-23

**الهدف:** لا تجميل للإصدار الحالي، بل إصدار جديد يقف بثقة بجانب متاجر العلامات الكبرى.
**القيد الثابت:** كل شيء داخل GitHub + Vercel. لا خدمات ولا شبكات توصيل خارجية.
**المعيار:** سريع على أبطأ اتصال، مقروء بثلاث لغات، يديره شخص غير تقني.

---

## 1. تقييم صادق للإصدار الحالي

| المحور | الحالة | الحكم |
|---|---|---|
| الكتالوج | 310 منتجات | ✅ |
| صور المنتجات | صورة واحدة لكل منتج من أصل **1774** متاحة | ❌ نقص جوهري |
| صفحة المنتج | صورة واحدة، فقرة إنجليزية، بلا خيارات | ❌ ضعيفة |
| خيارات اللون والمقاس | موجودة عند المورّد وغير مستخدمة | ❌ مفقودة |
| بطاقات العلامات | نص بلا شعارات | ❌ غير احترافية |
| نظام التصميم | لا سلّم خطوط ولا رموز تباعد ثابتة | ❌ صعب القراءة |
| ترجمة المحتوى | الواجهة ٣ لغات، المنتجات إنجليزية | ⚠️ ناقص |
| بنية الكود | `script.js` واحد بـ43KB، `catalog.js` 115KB يُحلَّل في كل زيارة | ⚠️ يثقل أول ظهور |
| العرض | كل الصفحة تُبنى في المتصفح | ⚠️ شاشة فارغة على الشبكة البطيئة |
| الثقة | لا شحن ولا إرجاع ولا ضمان ولا تقييمات | ❌ مفقودة |

**الخلاصة:** الأساس سليم تقنياً (سرعة، لغات، لوحة تحكم)، والنقص في **عمق المنتج** و**النظام البصري**. الإصدار الثاني يعالج الاثنين.

---

## 2. قرارات معمارية

### 2.1 بنية البيانات
اليوم يُحمَّل `catalog.js` كاملاً (115KB) قبل ظهور أول منتج. في الإصدار الثاني:

- **فهرس خفيف** — معرّف، اسم بثلاث لغات، سعر، علامة، قسم، صورة، شارة. يُقدَّم مضغوطاً (~25KB) ويكفي لعرض الشبكة والبحث والتصفية.
- **تفاصيل عند الطلب** — المعرض والمواصفات والوصف الطويل تُجلب عند فتح المنتج فقط.
- **نموذج المنتج الكامل:** `id, slug, brand, category, name{ar,en,tr}, summary{ar,en,tr}, specs[{key,value{ar,en,tr}}], gallery[{url,kind}], variants[{color,size,id}], price, oldPrice, sku, stock, badges[]`

### 2.2 أول ظهور للصفحة
شاشة أولى تصل جاهزة من الخادم بدل بناءٍ كامل في المتصفح، ثم يتولّى JavaScript ما بعدها. هذا أهم مكسب على الاتصال البطيء.

### 2.3 تنظيم الكود
`script.js` واحد يتحوّل إلى وحدات ESM أصلية بلا خطوة بناء: `catalog`, `cart`, `product`, `filters`, `i18n`, `ui`.

### 2.4 ميزانية أداء مُلزِمة
| المقياس | الحد |
|---|---|
| JavaScript | ≤ 60KB مضغوط |
| أول شاشة HTML | ≤ 30KB |
| ظهور أكبر عنصر (3G) | ≤ 2.5 ثانية |
| انزياح التخطيط CLS | ≤ 0.05 |

يُفرض باختبار يفشل إن تجاوزنا الحد.

---

## 3. المراحل

### المرحلة ١ — عمق المنتج ⏳ جارية
1. **استيراد 1774 صورة** مع تصنيف آلي: *لقطة منتج* (خلفية استوديو بيضاء) للبطاقات، *لقطة استخدام* للمعرض. التصنيف يقرأ إطار الصورة.
2. **معرض صفحة المنتج** — صورة رئيسية، شريط مصغّرات، تمرير باللمس، تكبير.
3. **جدول مواصفات مترجم** — استخراج الطاقة والسعة والمنافذ والأبعاد والوزن والتوافق من بيانات المورّد وعرضها بثلاث لغات، بدل الفقرة الإنجليزية.
4. **خيارات المنتج** — تجميع الألوان والمقاسات في منتج واحد بدل صفحات منفصلة.
5. **وصف عربي حقيقي** لكل منتج مبني على مواصفاته، لا ترجمة حرفية.

### المرحلة ٢ — النظام البصري
6. **خط عربي** مستضاف محلياً (woff2 مُجزّأ) — أكبر سبب منفرد في صعوبة القراءة.
7. **رموز التصميم** — سلّم مقاسات، تباعد، زوايا، ظلال، حركة. كل مكوّن يُبنى عليها.
8. **إعادة بناء المكوّنات** — الهيدر، التنقل، بطاقة المنتج، كتلة السعر، الأزرار، الألواح.
9. **شعارات العلامات** الحقيقية في البطاقات وصفحة المنتج.
10. **الواجهة الأولى** — مساحة أقل قبل أول منتج، وصورة بطل تتبدّل عبر الكتالوج.

### المرحلة ٣ — جودة التسوّق
11. صفحة قسم وصفحة علامة مستقلتان.
12. تصفية متقدمة (علامة، سعر، توفّر، مواصفة) وترتيب.
13. بحث باقتراحات فورية.
14. منتجات ذات صلة أسفل كل منتج.
15. سلة وطلب واتساب أوضح، مع ملخّص وتأكيد.
16. صفحات الشحن والإرجاع والضمان — **تحتاج سياستك**.

### المرحلة ٤ — لوحة التحكم
17. تعديل جماعي للأسعار والتوفّر.
18. ترتيب المنتجات وإبرازها.
19. مدير وسائط: رفع وترتيب صور المعرض.
20. سجل تغييرات ونسخ احتياطية ظاهرة.

### المرحلة ٥ — الإثبات قبل الإطلاق
21. ميزانية الأداء مفروضة باختبار.
22. تدقيق وصولية WCAG AA.
23. مصفوفة أجهزة ومتصفحات حقيقية.
24. جولة مراجعة نهائية.

---

## 4. ما يحتاج قراراً منك

| البند | لماذا |
|---|---|
| سياسة الشحن والإرجاع والضمان | لا تُكتب نيابةً عنك، وغيابها يضعف الثقة |
| ~~حقوق شعارات العلامات~~ | ✅ مؤكَّد: وكلاء موزّعون معتمدون، فعرض الشعارات الرسمية مشروع |
| مراجعة الأسعار المستوردة | قبل الإطلاق |
| ~~رقم واتساب~~ | ✅ يُضبط من لوحة التحكم — الافتراضي `963949951985` إلى أن تغيّره |
| المنتجان التجريبيان | `Power bank` و`باور بنك 24w` بلا صور في مقدمة المتجر |
