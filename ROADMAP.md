# SYRIATECH — أين وصلنا، وما بقي

آخر تحديث: 2026-09-23

**القيد الثابت:** كل شيء داخل GitHub + Vercel. لا خدمات ولا شبكات توصيل خارجية.
**المعيار:** سريع على أبطأ اتصال، مقروء بثلاث لغات، يديره شخص غير تقني.

---

## 1. حالة المتجر اليوم

| المحور | الحالة | الحكم |
|---|---|---|
| الكتالوج | 312 منتجاً، لكل واحد صورة حقيقية | ✅ |
| مكتبة الصور | 1929 صورة مستوردة ومصنَّفة | ✅ |
| صفحة المنتج | معرض، مواصفات مترجمة، ألوان، بدائل، منتجات ذات صلة | ✅ |
| خيارات اللون | ألوان المنتج الواحد في بطاقة واحدة لا في بطاقات متفرّقة | ✅ |
| بطاقات العلامات | 7 شعارات رسمية | ⚠️ تُضاف مع كل علامة جديدة |
| نظام التصميم | `assets/tokens.css` مصدر واحد للألوان والمسافات والخطوط | ✅ |
| الخط العربي | Cairo مستضاف محلياً ومقتطع على حروفنا — 39 ك.ب للغات الثلاث | ✅ |
| ترجمة الواجهة | ثلاث لغات، لا نص مكتوب داخل الكود، يفرضه اختبار | ✅ |
| ترجمة المنتجات | الأسماء والأوصاف بثلاث لغات | ✅ |
| أول ظهور للصفحة | الصفحة الأولى وصفحات الأقسام والمنتجات تصل مبنيّة من الخادم | ✅ |
| الطلب | سلة ← إتمام الطلب ← رقم طلب ← إيصال ← سجلّ في اللوحة | ✅ |
| لوحة التحكم | منتجات، طلبات، زبائن، أقسام، محتوى، وسائط، مستخدمون، تقارير، نسخ احتياطية | ✅ |
| سياسات الشحن والإرجاع والضمان | 40 إجابة من 52 مكتوبة | ⚠️ **تنتظر 12 إجابة منك** |
| التقييمات | غير موجودة | ⬜ لاحقاً |

---

## 2. الميزانية المُلزَمة

يفرضها اختبار يفشل إن تجاوزناها — لا تقدير ولا وعد.

| المقياس | الحد | القياس اليوم |
|---|---|---|
| JavaScript على الصفحة الأولى | ≤ 60 ك.ب مضغوط | **58.6** |
| انزياح التخطيط CLS — الصفحة الأولى | ≤ 0.05 | **0.0002** |
| انزياح التخطيط CLS — صفحة المنتج | ≤ 0.05 | **0.032** (هاتف) · **0.001** (شاشة) |
| الخطوط | — | 39 ك.ب للغات الثلاث |

صفحة المنتج والسلة ولوحة التحكم لا تُحمَّل إلا عند فتحها، وكل واحدة تحمل نصوصها معها.

---

## 3. ما بقي

### ينتظرك أنت
1. **12 إجابة في سياسات الشحن والإرجاع والضمان** — تُكتب من تبويب **المحتوى** في اللوحة.
   السؤال الذي لا إجابة له لا يظهر للزبون أصلاً، فالصفحة صحيحة الآن لكنها ناقصة.
2. **مراجعة الأسعار المستوردة** قبل الإطلاق — هي أسعار المورّد لا أسعارك.
3. **رقم واتساب** — يُضبط من الإعدادات؛ الافتراضي `963949951985` إلى أن تغيّره.
4. **أسعار التوصيل لكل محافظة** — من الإعدادات. المحافظة بلا سعر تظهر للزبون "يُحدَّد لاحقاً".

### العلامات الجديدة
Joyroom · UGREEN · Beausu — لكل واحدة: شعار في `assets/brands/`، منتجاتها في الكتالوج،
ووصف قسمها بثلاث لغات. الطريق نفسه الذي مشته العلامات السبع الحالية.

### التوسّع المخطَّط
الهواتف · الاتصالات · الكمبيوتر · الطاقة الشمسية. شجرة الأقسام تتسع لها من اللوحة
(تبويب **الأقسام**) بلا لمس الكود — وهذا كان أحد أسباب بنائها هكذا.

### تحسينات مؤجَّلة عن قصد
- **تقييمات الزبائن** — تحتاج زبائن أولاً.
- **الدفع الإلكتروني** — اليوم الدفع عند الاستلام. البنية جاهزة لطريقة ثانية حين تتوفّر.
- **بريد تأكيد الطلب** — الكود جاهز؛ يحتاج `RESEND_API_KEY` وهو الاستثناء الوحيد خارج
  GitHub و Vercel، ويعمل المتجر كاملاً بدونه.

---

## 4. كيف تبقى الجودة

```bash
npm run build   # يولّد الشكل والنصوص والبصمات
npm test        # ثمانية اختبارات: الإعداد، التصميم، المحتوى، اللغة، الخادم، المتجر، الواجهة، التوافق
```

الاختبارات لا تتصل بأي خدمة خارجية ولا تمسّ بيانات المتجر الحقيقية.
أي تعديل قبل الرفع يمرّ من هنا: الاختبار الأحمر يعني أن شيئاً انكسر، لا أن الاختبار قديم.
